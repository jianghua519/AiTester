server:
  host: "0.0.0.0"
  port: 8001
  mode: "debug"

database:
  host: "postgresql-service"
  port: 5432
  user: "aicd_user"
  password: "aicd_password"
  name: "aicd_dev"

jwt:
  secret: "your-secret-key-change-in-production"
  expiration: 24
